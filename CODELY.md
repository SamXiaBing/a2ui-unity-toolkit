

## Codely Structured Memories

### User

### Feedback

### Project
- [2026-09-03 22:50:09] - 团结引擎 UITK 文本测量固化 bug（2026-08-30 修复，a2ui-unity-toolkit）：嵌套容器里的 Label/Button 首次布局被测出过小内容高度（19px 行高测成 0~8px）且 Yoga 缓存永久固化——改样式/改文本/重挂载/display 切换均不触发重测，表现为行距拥挤重叠。旧 absolute+left/top 拖拽因重建布局而掩盖，改 translate 拖拽后显形。修复模式（A2uiV08CatalogMapper.FixTextMeasure）：布局后逐 TextElement 用 MeasureTextSize（布局外测得正确值，签名 VisualElement.MeasureMode.AtMost/Undefined，反射不可见但编译可用）对比 layout.height，不足者 inline minHeight 顶高；必须退避重跑（2ms 起步×2.5 至多 8 轮）因为动态字体图集渐次补字期间 MeasureTextSize 也返回错值。陷阱：IVisualElementScheduler.StartingIn 单位是毫秒不是帧。figma-export 主题因渲染分支不同（scopeClass 挂 overlay、无 a2ui-skin--crafted）天然避开。2026-09-03 增补：①FindByKey + NormalizeTheme 已做 figma 别名规范化（ResolveFigmaAlias：剥分隔符后缀匹配取最短候选，EndsWith 而非 Contains 以防更长皮肤名劫持；NormalizeTheme default 分支先问注册表再折叠 ds，无递归）；②FixTextMeasure 已扩到 TextElement（覆盖 textfield input 内部文本），豁免 a2ui-textfield--long 多行框；③回归网已加纵向检查 ScanOverflow VCRAMP（盒高 < need-2 即报）——布置陷阱：必须放在 InClippingContainer 早退之前，覆盖层内容整体包在 a2ui-overlay-card__scroll（ScrollView）里，放早退之后会被全量架空；④main-mount 与 A2uiLauncherSurfaceHost.Render 已接 ScheduleTextMeasureFix；Tabs 换页/Modal 展开/MultipleChoice 筛选重建的动态子树在展示时单独补测；测宽用 content box 扣水平 padding/border（mapper 与 ScanOverflow 同式）。2026-09-03 对抗审查 3 员发现已全部分诊，终扫 55 样本 × ds + 全控件台 × 4 主题 0 issues。已知不修（记录为局限）：launcher 宿主不支持 figma 皮肤（无 figma USS 加载，功能缺口单独立项）；Styles 后补导入 USS 到域重载前不可见（缓存戳只看根目录）；窗口 resize 后 minHeight 失配（车机定分辨率场景罕见）。**Why:** 引擎级缺陷，未来改 UI 渲染/拖拽/皮肤极易复发或误删兜底。**How to apply:** 排查 UITK 文本挤压先对比 layout.height vs MeasureTextSize（exec_runtime_script 协程探针）；新增渲染路径（新宿主/新皮肤分支）记得接 ScheduleTextMeasureFix；ScanOverflow 里加新检查勿放 InClippingContainer 早退之后。



- [2026-08-31 16:49:53] - [2026-08-31 16:52:11] Tuanjie UITK 滚动坑（2026-08-31 修复 a2ui-unity-toolkit）：普通 VisualElement 的 overflow:auto/hidden 只裁剪不产生滚动条，UITK 里只有 ScrollView 自带 Scroller——Live 覆盖层长内容此前被内卡 overflow:hidden 直接裁掉看不见。修复模式：内卡 max-height:100% + 条带用 top/bottom 定高（percent max-height 需要父级 definite height 才能解析，纯 max-height 的 auto 父级下会被 Yoga 当 undefined），内容包 ScrollView.a2ui-overlay-card__scroll；A2uiDragManipulator 已跳过 Slider/Scroller/ScrollView 不冲突。另：v0.9 双栈下 Normalizer 需把 action:{event:{name,context:{}}} 转成内部 {name,context:[{key,value}]}、Button variant:"primary" 转 primary:true，Mapper 本体无版本感知。**Why:** 引擎级坑，改覆盖层/宿主布局极易复发。**How to apply:** UITK 要可滚动必须上 ScrollView；percent 尺寸先确认父级高度 definite。
- [2026-08-31 20:24:22] - [2026-08-31 21:05:20] Tuanjie 编辑器截图取帧三条路实测（a2ui-unity-toolkit 视觉校准管线）：①CaptureScreenshotAsTexture 返回空纹理不可用；②ReadPixels+WaitForEndOfFrame 读到全黑背缓冲；③唯一可用是 ScreenCapture.CaptureScreenshot（异步写盘）。且 Codely 桥会话中偶发 custom_tools_reloaded 重载导致脚本双执行/日志丢失 → 异步写盘与双协程竞态会产生跨帧串味截图（card.png 内容是别的组件）。可靠配方：删目标文件→CaptureScreenshot→轮询文件出现且尺寸连续两次一致→再请求一张哑截图做 FIFO 刷屏屏障→才继续下一状态；结果以磁盘哈希唯一性验证，不信任返回日志。Unity 屏幕像素≠UI 面板像素：PanelSettings 参考分辨率 1920 在 1280 屏上缩放 2/3（640 面板px 卡片显示为 425 屏幕px），截图测量需按卡宽反推缩放。**Why:** 这些 API 行为查不到文档，全靠实测排雷。**How to apply:** 任何 Unity/Tuanjie 编辑器内自动化截图/像素测量任务直接用此配方，勿再试①②。
- [2026-08-31 20:24:22] - [2026-08-31 21:05:24] Figma 视觉校准管线状态（a2ui-unity-toolkit，2026-08-31 建成）：PAT 在 D:\AIWorkSpace\.figmakey；标准模板文件 key bl985lo1stxpBBV94SQlY6、画布 16:73 "A2UI Components"、组件节点命名规范 '别名 / Type:variant'（如 playBtn / Button:primary）；images API 返回键带原始冒号、scale=2 即 2 倍设计像素。工具链：figma_api_export.py --no-convert 拉 JSON、figma_visual_diff.py 出几何比+主色差+设计空间 1:1 并排图（TestResults/figma_calib/diff/pairs/）。M3Template 主题（Styles/M3Template，figma-m3template）由名字规范驱动转换生成。**未决缺陷：** Unity 渲染文本周围有 #E9EAEA 灰带（非抗锯齿、非 FixTextMeasure，标签本身 68 面板px/字号 44 正确），把文本内容盒撑到 ~2.2 倍，导致文本类几何比虚高（h1 实测 0.4 应为 1.0）；btnS 次按钮底色疑似未画出（inkΔ=120）。**Why:** 跨渲染器像素级 diff 被字体噪声支配，正确度量是设计空间几何比+主色差。**How to apply:** 灰带根因待查（嫌疑主题某条 text background 规则）；修好后把 12 组件 ratio/inkΔ 阈值固化进 metrics 判定即全自动回归。
- [2026-09-01 18:39:28] - [2026-09-01 18:40] Tuanjie ScrollView 三坑（a2ui-unity-toolkit 兼容矩阵已收录，2026-09-01 实测）：①mode=Vertical 不隐藏横向 Scroller（实体化占 24px），必须 horizontalScrollerVisibility=Hidden；②ScrollView.ScrollerVisibility 嵌套枚举不存在——枚举在顶层 UnityEngine.UIElements.ScrollerVisibility（写嵌套报 CS0117）；③ScrollView 自带灰边框圆角 chrome，嵌自定义主题卡显形，需 USS 复位（透明底+border 0+隐藏 scroller）。另：border-radius 横纵独立钳制，999px pill 会拉成椭圆，USS 生成必须钳到 height/2；Mapper 文本类名 a2ui-text--<hint> 动态拼，皮肤 USS 必须生成全部 8 个字号变体否则回退基础字号（曾致 h2/h4 渲染成 20px）。**Why:** 视觉校准管线抓出的三个真缺陷，全是引擎行为+生成器盲区叠加。**How to apply:** 改 figma_to_uss.py 或主题 USS 时先读兼容矩阵 ScrollView 节。
- [2026-09-01 18:39:29] - [2026-09-01 18:41] Figma 视觉校准管线完成态（a2ui-unity-toolkit）：figma_visual_diff.py 度量 12 组件 = 设计空间几何比 + 主色差（不做逐像素 diff，字体噪声支配），Figma PNG 用 images API scale=2（节点即精确边界），Unity 侧 ScreenCapture+哑截图屏障配方；关键教训——像素级"灰带"诊断最后发现是卡片圆角 AA 弧+ScrollView chrome，度量被污染时先读 resolvedStyle/layout 权威数据再修像素算法。最终 9/12 OK：btnS/caption inkΔ 是 AA 伪影（resolved bg #E8DEF8 / 色 #49454F 逐位精确），slider 是语义差（拖柄未主题化=已知缺口）。校准基线与判定阈值固化在 figma_visual_diff.py THRESH 表 + docs/figma_pipeline_status.md 校准章节。**Why:** 用户人眼标定已给出三条意见（椭圆/分割线/尺寸差），全部落实或定性。**How to apply:** 后续换新 Figma 稿→重跑转换→重录 12 截图→跑 diff 对比 THRESH 即全自动回归；新组件加进校准集需同步 SKIP_GEO/SKIP_INK/THRESH。
- [2026-09-01 22:32:23] - [2026-09-01 23:05] Tuanjie USS 解析器重大坑（2026-09-01 实锤，a2ui-unity-toolkit）：**var() 带逗号 fallback 的声明被静默丢弃**——`font-size: var(--a2ui-type-h2, 36px)` 整条不生效（属性回落引擎默认 14px），同规则内无 var 的属性（如 flex-shrink）仍生效，极难排查；`var(--x)` 无 fallback 形式正常。曾致 M3 a/dark 主题全部字号 14px（用户报"文字模糊"），DS 幸免因其别名层用字面量。修复：Crafted.uss 34 处 font-size var-fallback 全改字面量（字号全主题统一，无主题化损失）。诊断方法论：颜色被内联 ink 掩盖时，用 resolvedStyle.fontSize/flexShrink 等判据区分「USS 没挂上」vs「声明被丢弃」（shrink=1+fs=14 → 同规则选择性丢弃 → var 语法问题）。**Why:** 引擎级静默失败，兼容矩阵原「var() ✅」记载是错的已修正。**How to apply:** 本仓库 USS 一律字面量字号/间距；颜色 var() 异常先查 fallback 逗号；新主题 USS 用字面量或无 fallback var。
- [2026-09-04 17:03:01] - [2026-09-02 15:10] a2ui-unity-toolkit 提交基线（2026-09-01/02 三连提交，工作区另有未提交修复）：80887b9 feat（v0.9 双栈收尾+视觉校准+瘦身）、8d74af3 docs（GitHub 发布准备：THIRD-PARTY-NOTICES/CONTRIBUTING/CI/迁移脚本/双语 README）、c4a5443 fix（M3 字号 14px var-fallback 根因、FigmaExport 紫色 ink 回归、垂直拖拽）。**未提交**：MapButton 回退事故5契约、NormalizeTheme 去 figma-* 透传、Validator catalogId 严格化、兼容矩阵/状态文档更新——全量回归 275 组合仅剩 5 个 VCRAMP（CheckBox 标签/输入框文本 15px 级高度不足，MiSans 行高 1.6em vs 容器 1.2em，疑似 Unity 内部控件默认样式，chk_probe 已写未跑）。**Why:** 下次会话接手时需要知道基线在哪、什么已修什么悬着。**How to apply:** 先跑 chk_probe（Temp/chk_probe.csx）定位 VCRAMP 高度来源→修 Crafted.uss→重跑全量回归→绿了执行 prepare_github_release.py（SamXiaBing 身份、main、v0.1.0）出发布仓库。
- [2026-09-04 21:04:15] - [2026-09-02 18:50] a2ui-unity-toolkit VCRAMP 假阳性根因与修复（2026-09-02）：MeasureTextSize 含 MiSans 行距（1.65em），渲染盒只有字形度量（1.2em），20px 中文标签 h=24/need=33 被误报——fix_a.png 肉眼证实无裁剪。修复双管：①MapCheckBox 结构化拆分（Toggle 只做 36×36 勾选盒，文字用独立 a2ui-text 兄弟标签，不再塞进定尺寸盒）；②A2uiTestApi VCRAMP 阈值改比例式（h < need×0.6 才算塌陷；历史事故 0.35、行距伪影 0.73，0.6 干净分界）。附带：GetThemeInk 对 figma-* 返回 null（纯 USS，FigmaExport 橙色不再被内联紫覆盖）、NormalizeTheme 未知 figma-* 回落 ds 不透传。**Why:** 度量伪影与真缺陷（0.35 vs 0.73）区间可分，阈值固化后全自动回归不再误报。**How to apply:** VCRAMP 报警先算 ratio 再动手；改 Toggle 结构时同步查 Crafted 的 .a2ui-checkbox 盒样式假设。
- [2026-09-04 21:04:15] - [2026-09-02 18:51] a2ui-unity-toolkit 发布仓库已生成待推送：D:\AIWorkSpace\a2ui-unity-toolkit-release（干净单提交 259e87b，SamXiaBing 身份，main + v0.1.0 tag，含润色后双语 README 与全部修复）。生成脚本 Tools/prepare_github_release.py（git archive 导出 HEAD，tar 解包，个人身份单提交）。用户下一步：GitHub 建空仓库→push→Release→topics（unity/tuanjie/a2ui/genui/agent/llm/ui-toolkit/cockpit）。注意主仓库交互编辑器开着时 batchmode 回归会因项目锁失败并报陈旧结果（layout_summary 不更新即假象）。**Why:** 发布就绪状态与操作残留未完成。**How to apply:** 用户 push 后此条可删；回归失败先查 layout_summary 时间戳是否新鲜。
- [2026-09-05 11:00:06] - [2026-09-05 10:45] a2ui-unity-toolkit 已正式开源上线：https://github.com/SamXiaBing/a2ui-unity-toolkit（main = 8179ecb，v0.1.0 Release 已发布，双语 README 顶部语言互跳，CI uss-lint 绿）。发布仓库在 D:\AIWorkSpace\a2ui-unity-toolkit-release（dev 仓库无 remote，推送走 release 仓库）。遗留：①CI uss-lint 首轮曾报 var-fallback（已修 192 处 Crafted.uss 剥离但 A2uiAlias.uss 5 处的剥离未提交到 release 仓库——下次 push 需同步）；②About topics 用户尚未填；③slider 拖柄未主题化。**Why:** 开源发布是里程碑，后续会话需要知道仓库已公开、地址、以及遗留项。**How to apply:** 后续改动走 release 仓库推送（不要推 dev 仓库 master——无 remote）；A2uiAlias.uss 的 5 处 var-fallback 剥离需同步到 release 仓库。
- [2026-09-07 14:52:11] 公众号/知乎长文（mp UFqDQDhYucKRuFd-JyHMOg / 知乎 p/2079675042464523426）已反向校准两仓 README：回归数 500+→约280（5×56）、新增「已知局限」章节、安全表补 A2uiPolicyGate 座舱情境门禁（D/R 挡且速>0.5 拦截 Tabs/Modal/List/MultipleChoice/Video/DateTimeInput 改写简化模板）、修英文 README THIRD-PARTY 链接笔误。文章数字口径核实全真（23 C#=Runtime、18 py=Tools 顶层、56=jsonl 总数）。文章"用 Tuanjie或者Unity 打开"已按用户决定改写：归档版用「用 Tuanjie 打开（Unity 2022.3 兼容 fork，标准 Unity 未验证）」，平台修正文案已交付用户自改；55t4（兼容实测底座）vs 62t13（当前要求）定性为非冲突不动。英文版已落地（仓库存档+平台发布方案）：两仓 docs/article.zh-CN.md（定稿归档，图片改仓库本地 article-assets/1~5.png 防 OSS 签名过期）+ docs/article.en.md（英译），README「重要文档/Key documents」首条为仓库存档双链+平台原载链接。两仓已同步（README 各 +15/-2 + docs 三个新条目），已于 2026-09-07 提交并随 D1 feature 推送 GitHub（41f8bb2）。**How to apply:** dev.to/Medium 发布后需把平台链接补进两仓 README 条目；提交走 release 仓库。

- [2026-09-07 11:38:11] [2026-09-07] a2ui-unity-toolkit 月度迭代计划 v3 落盘 docs/development-plan-2026-09.md（2026-09-07→10-12 共 20 工作日+9/20、10/10 调休弹性位，目标 v0.2.0）：W1 协议安全上限+官方 error 封套+真 v0.9.1 字段+滑柄主题化 → W2 交互闭环（输入双向绑定/Modal 遮罩焦点/DateTime 三段下拉）→ W3 流式 TTFC+Video/Audio 真播放 → W4 列表 ListView PoC 或分页保底+内置函数全集+callFunction 拒绝路径+conformance v0.9.1 76 条数据驱动移植+发版。Day1(9/7)=消息 1MB/组件 1000/surface 50 三上限收进 A2uiV08Validator 单点（覆盖 A2uiSchemeAHost L639 + A2uiLauncherSurfaceHost L331 双入口）。9/25 中秋、10/1-10/7 国庆已排开，D20=10/12。基线冻结点 D5/D9/D14/D18=预发布 tag v0.1.1/v0.1.2/v0.1.3。**Why:** 计划经两轮独立"2 调研+1 起草+3 对抗审查"（v2 由并行会话 10:40 产出；本会话 11 点档独立重跑同流水线，两轮 blocker 完全收敛，增量合并为 v3：surfaceId 唯一性、callFunction 必答拒绝路径、回写语义=本地回写+action 上报无 changeValue 事件、弹层作用域/detached 挂类坑、C# 内联样式优先、周更 tag、调休坐实），后续会话按日执行验收。**How to apply:** 接手先读该文档对应当日条目；参考仓库浅克隆在 %TEMP%\a2ui-ref（官方）与 %TEMP%\a2ui-android-ref（社区 Compose）。

- [2026-09-07 11:35:11] - [2026-09-07 11:45] a2ui-unreal-toolkit 移植项目启动（2026-09-07）：A2UI 协议 Unreal 渲染方案，新仓库 D:\AIWorkSpace\a2ui-unreal-toolkit（Unity 仓库只读不动）。引擎 UE 5.6.0 源码版 D:\UnrealWorkspace\unrealengine-5.6.0（Editor/Cmd/Build.bat 已验证，VS18 MSVC）。计划 docs/PLAN.md v1.1（11 章）+ docs/REVIEW.md（三路红队 17 项发现：2 阻断=①-nullrhi 下 Slate.SkipWidgetDrawingInHeadlessMode 默认 1 会跳过 prepass/tick 致 GetTickSpaceGeometry 恒零，须 DefaultEngine.ini [SystemSettings] 置 0；②RequestExit 带不了退出码，成败判定走 selftest_result.json + RequestExitWithStatus(true, clamp(0-255))）已提交 6783ec4。样例 56 jsonl + 24 USS 底本已拷入新仓库（Samples/、Tools/theme_src/）。A 级引擎无关层约 1300 行（Processor/Normalizer/Validator/ActionRouter/SessionRecorder/PolicyGate）逐行转写 C++，CatalogMapper 构树逻辑转写+产出物换 UMG，FixTextMeasure 等引擎特定兜底丢弃。**Why:** 用户要求换引擎不重构、保真转写、headless 全自动回归 5 主题×55 样例=275 组合全绿。**How to apply:** 后续会话接手先读新仓库 docs/PLAN.md 与 REVIEW.md；实现进行中（S1 骨架+M0 spike、C1 Core 转写、C2 主题转换器并行，Renderer/Host/Tests 待排）；L2 回归命令与产物格式在 PLAN §6.2/§7（地图须全路径 /Game/Maps/A2uiDemo、VCRAMP 用 STextBlock GetDesiredSize 而非单行 Measure、豁免链类型判定优先）。
- [2026-09-07 12:02:35] - [2026-09-07 12:05] 0x800711C7「应用程序控制策略已阻止此文件」根因实锤：本机 Windows **Smart App Control 处于 On**（Get-MpComputerStatus），CodeIntegrity 日志 3077/3033 显示其拦截未签名 .NET DLL（UE 的 EpicGames.IoHash.dll、历史上的 Tuanjie Bee.Stevedore.Program.dll）。SAC 云信誉评估网络抖动=「间歇性」假象；SAC 无排除机制，UBT 类构建工具在 SAC On 下无法运行，唯一解是用户在 Windows 安全中心→应用和浏览器控制→关闭 SAC（**不可逆，关后无法重开**，需重装系统才能恢复）。**Why:** UE C++ 构建管线硬依赖 UBT；此坑会反复咬所有未签名 .NET 构建链。**How to apply:** 遇 0x800711C7 先查 SAC 状态再怀疑 AV；需要用户手动关闭时明确告知不可逆性。
- [2026-09-07 12:42:22] - [2026-09-07 12:40] a2ui-unreal-toolkit 实现就绪待构建（2026-09-07）：全部代码已写完并提交（main: 5c0397b→0683867→a5f03bf）——Core 层 11 文件逐行转写（FA2uiJson 封装 API：At/AsStringLoose/DeepClone 等；Bound 双 Resolve 顺序；Processor/Normalizer/Validator/PolicyGate/ActionRouter/Recorder/ThemeRegistry）、FA2uiCatalogMapper（22 组件→UMG）、FA2uiStyleSet（主题 JSON+figma 缺变体回退链）、UA2uiSurfaceWidget（G0→G4→Build→动作回灌闭环，卡锚点 a2ui-overlay-card__inner）、L1 单测（commandlet Main 返回值携码）+L2 扫描器（类型判定豁免/VCRAMP GetDesiredSize）+275 组合 ticker 状态机+run_selftest.ps1（result.json 判成败）。唯一阻塞：Smart App Control 仍 On，用户去关闭中。**关键教训：①本会话 subagent 系统性不可用**（6 连败，均在 8-35 次调用后 "Request timeout ~252s/ERROR" 终止、调研完零产出）——大段代码产出任务应主会话亲自分块写；②USS 转换器三坑：CSS 自定义属性名大小写敏感（小写化致 var 全灭）、复合选择器 .a.b 需先分解、Crafted.uss 选择器不带 skin 作用域前缀（由挂载点隐式提供）；③UMG 动态重建树：NewObject(Outer)+根 UPROPERTY+RemoveFromParent 才能 GC，勿用 WidgetTree->ConstructWidget（Outer 链泄漏）。**How to apply:** 用户关 SAC 后直接跑 Tools/run_selftest.ps1 -Build；编译错修复→spike→L1→L2 标定+275 组合→全绿出报告；残缺项：A2uiHostActor 仍用 spike widget（未换 SurfaceWidget）、控制台命令 A2ui.LoadSample/Theme/Gate 未注册在 Host（Gate API 在 SurfaceWidget.SetGate）、demo 地图未建（L2 用引擎 Entry 地图）。
- [2026-09-07 14:34:41] [project] 2026-09-07 用户已关闭 Windows Smart App Control，为 Unreal 5.6 源码版（D:\UnrealWorkspace\unrealengine-5.6.0）UBT 构建与自编译二进制（UnrealEditor-Cmd 等）运行扫清拦截。**Why:** a2ui Unreal 移植需要 headless 构建+自测，SAC 可能拦未签名可执行。**How to apply:** 若后续构建/命令行运行仍报拦截，先查亚信安全 AV 而非 SAC。
- [2026-09-07 15:43:17] a2ui-unity-toolkit 的 Unreal 5.6 移植仓库已存在于 D:\AIWorkSpace\a2ui-unreal-toolkit（前会话产出，6 个提交）：PLAN.md v1.0→红队对抗审查（REVIEW.md，3 路 17 项修正）→PLAN v1.1、A2uiCore 逐文件转写（FA2uiJson 包 UE Json 模块）、FA2uiCatalogMapper 22 组件+FA2uiStyleSet 主题桥+UA2uiSurfaceWidget、L1 commandlet+L2 -game 双层自测（Tools/run_selftest.ps1，产物 TestResults/selftest_result.json+layout_summary.txt，275 组合）。引擎=D:\UnrealWorkspace\unrealengine-5.6.0 源码版。截至 2026-09-07 尚无 TestResults（自测未跑过）；本会话续做：UBT 首轮过了文件名冲突+~12 类编译错（checkpoint f261fcb），正在构建迭代。**Why:** 多会话并行推进同一任务，接手前必须先查该仓库 git log 避免重做/覆盖。**How to apply:** 后续 Unreal 移植工作直接在此仓库续做（勿在 unity 仓库动手）；剩余里程碑=构建绿+275 自测全绿+Demo 地图（Content/Maps/A2uiDemo 尚缺，L2 暂用 /Engine/Maps/Entry+默认 GameMode 兜底）+终报。
- [2026-09-07 16:28:01] A2UI 协议 Unity 源语义裁定四则（2026-09-07 L1 对抗调试实测，Unreal 转写与 Unity 月度迭代都会用到）：①ValidateJsonl 对"多消息合并一行"按 MessageKeys 拆分后**接受**（TrySplitMultiMessage 容错，源 A2uiV08Validator.cs L113-137），不是拒绝；②TryGetComponentType = 先键查 componentDef["component"] 再取**内层**首属性名/值（外层还有 "id"，取外层首属性是错译，源 Processor.cs L297-311）；③HandleDataModelUpdate 无 path 时 contents 的 key 是根对象**字面属性**（key="/a/b" 就是叫 "/a/b" 的键，不是路径！），带 path 时 contents 整体落到该路径（DataModel.deep.x={"k":v2} 形态）；GetByPath 前导"/"剥层+空段跳过（源 Processor.cs L236-260）；④G4 改写模板逐字契约：ids root/col/msg/hint、Card child="col" **字符串引用**、h2「行驶中已简化界面」+ caption「请停车后再查看完整内容」、Column alignment=center、beginRendering 带 catalogId。**Why:** 前会话转写按 C# 直觉写错这四处，L1 五断言失败全源于此；另外 figma-figmaexport 基字号=16（不是 M3 的 36），变体回落断言要按主题数据实值。**How to apply:** 改两仓协议代码/校验器/写测试期望时，先对照上述 C# 源位置，勿凭直觉；Unreal 侧修复已随构建迭代提交。
- [2026-09-07 17:19:43] UE5.6 headless UI 回归实测事实（2026-09-07 L2 调试，非文档可推导）：①REVIEW C-C1 的修正不完整——`Slate.SkipWidgetDrawingInHeadlessMode=0`（ini 或 ExecCmds 均确认=0）只解除 PrivateDrawWindows 早退，**-game -nullrhi 下 AddToViewport 挂载的 UMG 仍全零几何**（窗口 1707x960 存在、Slate 在 Tick、NativeConstruct 已触发，但子树 desired=0=从未被 prepass，widget 未进绘制树）；②解法三层缺一不可：专属 SWindow 直挂 + `Slate.AllowSlateToSleep=0`（unattended 无输入→Slate 睡眠→DrawWindows 永不执行）+ **`-RenderOffScreen` 命令行**（DrawWindowAndChildren 门控 `bRenderOffScreen||IsVisible`，unattended 窗口不可见→PaintWindow/arrange 被跳过→geo 恒零；离屏模式是引擎官方 headless 通路，已写入 run_selftest.ps1）；③已加 ZERO_GEOM 闸门（per-combo card 尺寸进 layout_summary/JSON，零几何显式判死）——之前一轮 275"绿"就是被该闸门揭穿的全零假绿；④UE5.6 API：SlatePrepass(float)（无 ForceLayoutPrepass）、TObjectPtr 无 IsValid()、UE_LOG 参数别放三目（格式消毒器报错）、两个 -ExecCmds 都会生效；L1 verdict 用退出码，stdout 捕获会抖动，以项目 Saved/Logs 落盘日志为准。**Why:** headless 几何回归的静默假绿是最危险的失败模式，这些事实花了两轮实验才钉死。**How to apply:** 后续 UE headless UI 测试直接用专属 SWindow+ZERO_GEOM 闸门配方；别再走 AddToViewport-nullrhi 路线。
- [2026-09-08 11:35:55] a2ui-unreal-toolkit L2 调试收官（2026-09-08，已全绿）：①L1 24/24 + **L2 275/275 真绿**（card=1232x640 真实几何非零，ZERO_GEOM 闸门确认非假绿；提交 2d0089f）；②最后一块根因：UMG **NativeConstruct/构造函数里构树都会失败**——NativeConstruct 时窗口绘制树已挂 SNullWidget 占位（子树游离永不 Paint），构造函数里 WidgetTree->ConstructWidget 则因构造期 NewObject 崩溃；**正解=改继承 UWidget + 覆写 RebuildWidget()**（UMG 官方代码构树入口，NewObject(this)+SetContent+返回根 TakeWidget()），Create 改 NewObject(World)（CreateWidget 是 UUserWidget 专用）；③工具链事实：run_selftest.ps1 组合命令里 build 失败后 if 块静默跳过导致整体 exit 0 假完成（要显式 BUILD_EXIT 判断）；TObjectPtr 无 IsValid() 用 != nullptr；④自测基础设施：spike 自杀已修（命令行含 A2ui.RunSelfTest 则跳过）、坏包保帧/越界/VCRAMP<0.6 扫描全走真几何。**遗留**：demo 专用 .umap 未建（PIE 用引擎 Entry 地图+HostActor 兜底）、控制台命令已注册无 UI 面板、VCRAMP 0.6 阈值未按 PLAN §6.4 复标（UMG 文本自动换行未触发挤压，伪阴风险低）、MiSans 未导入用默认字体。**How to apply:** 编辑器开着时勿跑 run_selftest.ps1（项目锁致 batchmode 假败）；用户已在编辑器打开工程实测（2026-09-08 11:34）。

- [2026-09-08 10:23:39] - [2026-09-08 10:30] a2ui-unity-toolkit D2+D3 完成（2026-09-08，未提交工作区）：①防护上限收进 A2uiV08Validator 单点——Path 深度 10（dataModelUpdate/updateDataModel path 段数 + contents/value 值树嵌套）、键长 50、错误条数 100（校验从首错即停改为收集至 100 截断）；②surfaceId 唯一性双层：Validator 载荷内 createSurface 未删先建拒绝 + Processor 跨载荷持久状态抛错（宿主对 v0.9 每帧 Clear 故不触发，v0.8 无 create 概念不受影响）；③D3 官方 error 封套 A2uiErrorEnvelope.Build（{version:"v0.9", error:{code=VALIDATION_FAILED, surfaceId, message, path=JSON Pointer}} 四字段全必填）+ 校验失败逐字段 Path + 双宿主 SendError/ErrorReported 独立通道 + RecordError 落 Temp/A2UISchemeA/sessions/errors_*.jsonl。验证：新单测 16 例、全矩阵 63/63 绿、layout_summary 275 组合 0 失败。**用户决策：theme.primaryColor 解析永久砍除**（v1.0 已删该字段，做了对未来无价值），计划文档 D2 行已记录，勿再加回。**Why:** W1 协议安全收口。**How to apply:** batchmode 跑 run_regression.py 截图 diff 步骤必 exit 1（batchmode 无截图目录，环境假象非回归失败）——以 results.xml 通过数 + layout_summary 组合数为准；D3 封套 path 对应被拒报文字段位置，agent 侧直接 parse errors JSONL 即可自纠。
- [2026-09-08 11:29:40] - [2026-09-08 11:00] a2ui-unity-toolkit 交互模式"推送成功但画面空白"根因（2026-09-08 用户报 catalog_all v0.8/v0.9 不渲染）：A2uiSchemeAHost.RenderOverlay 自初始提交就有 `_act != Act.Live || string.IsNullOrEmpty(_overlayPrompt)` 双门禁，空 prompt 即整卡隐藏；样例重组（951893a）给 54/56 样例补了 `# prompt:` 行、唯独 catalog_all 两个版本没补，TestPusher 发送 prompt="" → validation OK、消息全部入栈但被隐藏。batchmode 回归矩阵不受影响（测试传合成 prompt "# regression xxx"）。修复三件：门禁只看 Act、11 个宿主样例常量更新到重组后路径（media_card→features/media_player 等，映射经 surfaceId 核对）、catalog_all ×2 补 prompt 行。**Why:** prompt 是协议可选元数据，不能当显示条件；另一个坑是宿主常量在样例目录重组后成了死路径（Act 切换/默认样例加载会"加载失败"）。**How to apply:** 再遇"推送成功不渲染"先查 Editor.log 的 `RenderOverlay: hiding` 行和 prompt 是否为空；改样例目录结构必须同步宿主常量与 TestPusher 分组；用户编辑器开着本项目时 batchmode 验证必失败（项目锁），让用户聚焦编辑器重编译后直接在 TestPusher 复推验证。

### Reference
- [2026-09-05 20:58:25] [reference] a2ui-unity-toolkit 三款 AI 编程工具的聊天记录存储位置（2026-09-05 实测）：WorkBuddy 在 %USERPROFILE%\.workbuddy\projects\<工作区slug>\<sessionId>.jsonl（每行一个事件，type=message 取 role/content[].input_text，子代理在 <sessionId>\subagents\）；团结 Cowork/Codely 桌面版只有 %USERPROFILE%\.codely\sessions\<profile>\sessions.sqlite 的 session_metadata 索引（title/workspaceDirectory/messageCount/时间戳，正文不在本地，sqlite+wal 要拷贝到临时目录再读防锁），消息正文找不到（不在 WAL/EBWebView/logs）；CodeBuddy 聊天正文在云端，本地只有 %APPDATA%\CodeBuddy CN\User\globalStorage\tencent-cloud.coding-copilot\file-changes\<hash>\（文件改动 JSON 可判定某工具在某日期改过哪些文件，genie-history 目录为空）。本项目关键会话：Codely「A2UI技术解读」2026-07-24 n=30、主孵化 07-31→08-31 n=2030（metis 工作区）、独立库冲刺 89e62161 n=425 + 81114b1c n=1237；WorkBuddy 主力 d-UnityWorkspace-metis-Unity\42a4be6b（08-14→08-21）与 c-Users-xiabi-WorkBuddy-2026-08-17-11-41-16\2e163f64（08-17→08-19 DS 主题+Figma 转向，子代理含 A2UIDemo/A2UISchemeA 代码审计）。开源介绍文章：2026-09-05 用户两轮自改后终版标题《车机端生成式 UI 实践：A2UI 协议的 Unity UI Toolkit 原生渲染方案》，定稿 D:\AIWorkSpace\media\final\车机端生成式 UI 实践：A2UI 协议的 Unity UI Toolkit 原生渲染方案\同名.md（要点块置顶、无 H1、WebView 路线补 Vuplex 离屏纹理细节；5 张 mermaid 已由用户导出并传 OSS，文中引 CDN 图、本地留 1.png~5.png；封面 封面图.jpg（seedream 3525×1500=2.35:1，标题文字渲染正确，「实践」后有一个小引号状伪影）。回归组合数按 5 主题×56 修正为约 280（517 为主题裁剪前旧口径）；README 的 500+ 口径待同步修正。**How to apply:** 以后要挖开发史/写周报/找某决策出处，按上述路径直接取，别再从头探索；Codely 正文缺失时用项目记忆 md + git log + docs/ 替代；写回归主题数/样例数先现场数（5 主题×56≈280）勿沿用旧口径。



